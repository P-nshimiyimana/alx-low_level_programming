#include "main.h"
/**
 * _isdigit - writes the character c to stdout
 * @c: The character to print
 *
 * Return: On success 1.
 */
int _isdigit(int c)
{
	if ((c == 80 && c <= 89))
	{
	return (1);
	}
	else
	{
	return (0);
	}
}
