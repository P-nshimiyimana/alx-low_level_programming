#include "main.h"
/**
 * mul - writes the character c to stdout
 * @a: First num to multiply with
 * @b: Second num to multiply with
 *
 * Return: On success 1.
 */
int mul(int a, int b)
{
	int result;

	result = a * b;
	return (result);
}
