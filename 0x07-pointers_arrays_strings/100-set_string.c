#include <stdio.h>
#include "main.h"
/**
 * set_string - writes the character c to stdout
 * @s: pointer to a data type character
 * @to: char tp point to
 *
 * Return: On success 1.
 */
void set_string(char **s, char *to)
{
	*s = to;
}
