#include "main.h"
/**
 * _abs - print lower case
 * @c: The character to print
 * Return: 0 return successful executio
 */
int _abs(int n)
{
	if (n < 0)
	{
	return (-n);
	}
	else
	{
	return (n);
	}
}
