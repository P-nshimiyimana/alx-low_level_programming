#include "main.h"
/**
 * print_alphabet - print lower case
 *
 * Return: 0 return successful executio
 */
int _islower(int c)
{
	if ((c >= 97) && (c <= 122))
	{
	return (1);
	}
	else 
	{
	return (0);
	}
}
